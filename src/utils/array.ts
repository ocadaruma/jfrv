// export class Array {
//   // Search the element by binary search.
//   // Returns the index of the element x that satisfies floor(x)
//   static binarySearch(arr: number[], elem: number): number | undefined {
//
//   }
// }
